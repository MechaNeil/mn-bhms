import './bootstrap';

import Cropper from 'cropperjs';
import 'cropperjs/dist/cropper.css';

// Optionally, add the following global window binding if needed elsewhere:
window.Cropper = Cropper;

import './cropper-init';

